services:
  - type: web
    name: ninja-hattori
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.12.0
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
