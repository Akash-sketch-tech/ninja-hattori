# Port for Express server (Defaults to 3000 locally, 10000 on Render)
PORT=3000

# Secret key for JWT signing & verification
JWT_SECRET=ninja_hattori_super_secret_jwt_key_2026

# Optional custom SQLite database path
# DATABASE_PATH=./ninja_hattori.db
